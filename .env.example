# Deployment mode: "local" or "vercel"
# - local:  full features, requires backend server (server/index.js)
# - vercel: cloud mode, HTTP proxy as serverless function, WS client direct-connect, TCP disabled
VITE_DEPLOY=local
